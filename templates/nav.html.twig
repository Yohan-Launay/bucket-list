<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <img src="{{ asset('build/img/logo.png') }}" alt="Logo">
            <h1>Bucket-List</h1>
            <ul class="navbar-nav">
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link {{ currentPage == "/home"?'active':'' }}" aria-current="page" href="{{ path('app_home') }}">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{ currentPage == "/wish"?'active':'' }}" aria-current="page" href="{{ path('app_wish') }}">All wishes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{ currentPage == "/about"?'active':'' }}" aria-current="page" href="{{ path('app_about') }}">About</a>
                    </li>
                {% endif %}
            </ul>
        </div>
        <div>
            {% if app.user %}
                <p>Hello, {{ app.user.username }} !</p>
                <a href="{{ path("app_logout") }}">Déconnexion</a>
                {% else %}
                <a href="{{ path("app_register") }}">Inscription</a>
                <a href="{{ path("app_login") }}">Connexion</a>
            {% endif %}
        </div>
    </div>
</nav>